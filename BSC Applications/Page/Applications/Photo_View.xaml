<Page
    x:Class="BSC_Applications.Page.Applications.Photo_View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BSC_Applications.Page.Applications"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar Background="Transparent" Grid.Row="0">
            <AppBarButton x:Name="Open" Icon="OpenFile" Label="Open" Click="Open_Click" />
            <AppBarButton x:Name="Copy" Icon="Paste" Label="Copy" Click="CopyImage_Click" />
            <AppBarButton x:Name="Help" Icon="Help" Label="Help" Click="Help_Click" />
        </CommandBar>

        <Grid Grid.Row="1">
            <TextBlock x:Name="Message" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <Image x:Name="View" />
        </Grid>
    </Grid>
</Page>
