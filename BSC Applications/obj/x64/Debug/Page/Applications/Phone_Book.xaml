<Page
    x:Class="BSC_Applications.Page.Applications.Phone_Book"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BSC_Applications.Page.Applications"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local1="using:Windows.ApplicationModel.Contacts"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar Background="Transparent" Grid.Row="0">
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal" Spacing="15" Margin="0,4,0,0">
                    <TextBox x:ConnectionId='3' x:Name="Name" PlaceholderText="Name" Width="150" />
                    <TextBox x:ConnectionId='4' x:Name="Number" PlaceholderText="(xxx) xxx xxxx" Width="150" />
                </StackPanel>
            </CommandBar.Content>

            <AppBarButton x:ConnectionId='5' x:Name="Add" Icon="AddFriend" Label="Add"                   />
            <AppBarButton x:ConnectionId='6' x:Name="Save" Icon="SaveLocal" Label="Save"                    />
            <AppBarButton x:ConnectionId='7' x:Name="Open" Icon="OpenFile" Label="Open"                    />
            <AppBarButton x:ConnectionId='8' x:Name="Help" Icon="Help" Label="Help"                    />
        </CommandBar>

        <ListView x:ConnectionId='2' x:Name="List" BorderThickness="1" SelectionMode="None" HorizontalAlignment="Stretch" Grid.Row="1" />
    </Grid>

</Page>

